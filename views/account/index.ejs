<h1><%= title %></h1>

<% if (errors) { %>
  <ul class="notice">
    <% errors.array().forEach(error => { %>
      <li><%= error.msg %></li>
    <% }) %>
  </ul>
<% } %>

<% if (typeof messages === "function") { %>
  <%- messages() %>
<% } %>

<p>You're logged in</p>
